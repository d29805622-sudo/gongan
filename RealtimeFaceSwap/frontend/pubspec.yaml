name: realtime_face_swap

description: Real time AI video client

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
